/* 
Faça um programa com o loop FOR, que some todos os valores do loop, e
ao final mostre a média desses valores, e a soma

OBS: provavelmente tá erradokkkkk
     mas fiz.
*/

for (var n1 = 1; n1 <= 5; n1++) {
     var soma = n1 + n1
     var media = soma / 2
    console.log(`A soma entre ${n1} e ${n1} é ${soma}`)
    console.log(`A média dos números (${n1}, ${n1}) é ${media}.`)

}